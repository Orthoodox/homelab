
ARP — это протокол, который по запросу определяет MAC-адрес узла по его IP-адресу в локальной сети.

Он не сканирует сеть, а отправляет ARP-запрос только при необходимости (когда нужно отправить пакет).

Результаты сохраняются в ARP-таблице (кэшируются).
